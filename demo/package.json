{
  "name": "fontveil-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --emptyOutDir",
    "preview": "vite preview",
    "deploy": "pnpm run build && rm -rf ../docs && cp -r dist ../docs"
  },
  "dependencies": {
    "marked": "^18.0.5",
    "fontveil": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.9.2",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3",
    "vite": "^7.0.0"
  }
}
